1) При одноразовом использовании работает корректно. Можно сказать, что он - одноразовый
2) Пока флаг не установлен каждый поток будет зациклен в while(!ready_.load()) {}, а значит ни один поток не пройдет через вызов wait
3) Как только кто-либо вызовет set, ready_ изменится и все спящие уведомятся с помощью notify_all и пройдут через set.
4) Нет #include <atomic>
